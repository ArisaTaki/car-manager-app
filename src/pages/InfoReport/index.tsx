import React from 'react';
import classNames from 'classnames/bind';
import styles from './style.module.scss';

const cx = classNames.bind(styles);

const InfoReport: React.FC = () => (
  <div>
    <p>信息报表</p>
  </div>
);

export default InfoReport;
